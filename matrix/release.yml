apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: matrix
  namespace: matrix
spec:
  interval: 5m
  chart:
    spec:
      chart: matrix-synapse
      version: '~0'
      sourceRef:
        kind: HelmRepository
        name: matrix-charts
        namespace: matrix
  values:
