# Optional extra env (e.g. metrics); default install uses chart Secret …-env only.
apiVersion: v1
kind: Secret
metadata:
  name: stalwart-helm-env
  namespace: mailserver
type: Opaque
stringData:
  METRICS_USERNAME: "metrics"
  METRICS_SECRET: "REPLACE_ME"
